/*
 * Скрипт приймає 1 сторону (side) і діагональ (diagonal) прямокутника.
 Перевір це прямокутник (діагональ не може бути менше сторони прямокутника). 
 Поверни площу цього прямокутника або рядок not a rectangle, якщо це не прямокутник. 
 Якщо результат не ціле число, округли значення до 2 знаків після коми.
 */
// const side = 5;
// const diagonal = 6;
// let area = 0;

// if (diagonal < side) {
//   console.log('not a rectangle');
// } else {
//   area = side * Math.sqrt(diagonal ** 2 - side ** 2);
//   console.log(`Площа прямокутника дорівнює ${area.toFixed(2)}`);
// }
